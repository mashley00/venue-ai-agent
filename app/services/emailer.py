# Placeholder for SMTP/Graph email sender
def queue_and_send(venue: dict, payload: dict) -> dict:
    # implement later; return a status record
    return {"venue": venue.get("name"), "status": "queued"}
